'use client';

import { LibraryPage } from '@/views/library';

/**
 * Library Page
 *
 * File-system-like interface for managing workflows and prompts.
 * Uses full viewport height with its own sidebar.
 */
export default function LibraryDashboardPage() {
  return <LibraryPage />;
}
